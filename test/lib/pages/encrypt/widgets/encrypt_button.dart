import 'package:flutter/material.dart';

class EncryptButton extends StatelessWidget {
  const EncryptButton({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: () {},
      child: const Text("Cripta"),
    );
  }
}