import 'package:flutter/material.dart';

class ContactSelector extends StatelessWidget {
  const ContactSelector({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Contact selector (in costruzione)");
  }
}