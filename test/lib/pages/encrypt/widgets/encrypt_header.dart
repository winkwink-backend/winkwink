import 'package:flutter/material.dart';

class EncryptHeader extends StatelessWidget {
  const EncryptHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Header (in costruzione)");
  }
}