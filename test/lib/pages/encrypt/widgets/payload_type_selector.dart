import 'package:flutter/material.dart';

class PayloadTypeSelector extends StatelessWidget {
  const PayloadTypeSelector({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Payload selector (in costruzione)");
  }
}